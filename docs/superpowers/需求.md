main.py蓝牙配对逻辑改一下，扫描到设备后，先加入到队列中，如果扫描到ClaudeCodeIndicator这个名字，立即连接，否则只加入队列中；
如果没有扫描到ClaudeCodeIndicator，则在`扫描结束`或者`扫描开启10秒后`，启动连接逻辑；
──────────────────────────────────────────────────────────────────────────────────────────────────